name: Test Cron

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check date
        run: date
      - name: Echo test
        run: echo "GitHub Cron is working!"
